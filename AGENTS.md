# 项目根级 AGENTS.md

## 1. 项目目标

在 MATLAB R2024a 中实现并验证风光水火储统一单层连续优化模型的原始-对偶内点法，以及与完整稀疏 KKT Newton 方向严格等价的递推降阶解耦算法。先完成 7 日真实数据验证，再扩展到 365 日并行性能测试。

## 2. 每次任务开始时的强制阅读顺序

1. `README_从这里开始.md`
2. `CURRENT_STAGE.md`
3. `docs/00_用户确认的模型口径_v1.0.md`
4. `docs/02_最终模型合同_v1.0.md`
5. `docs/03_阶段模型启用矩阵.csv`
6. `docs/05_资料优先级与冲突处理规则.md`
7. 当前阶段目录下的 `AGENTS.md`、`阶段*_长任务.md`、`阶段*_验收矩阵.csv`
8. 与本次修改目录相邻的更深层 `AGENTS.md`

## 3. 资料优先级

用户确认模型口径 > 最终模型合同 > 当前阶段任务与验收 > 阶段 A 技术主依据 > Excel 数值 > 阳育德论文方法思想 > 旧双层模型历史资料。发生冲突时不得自行折中，按 `docs/05_资料优先级与冲突处理规则.md` 处理。

## 4. 已冻结的模型口径

- 单层统一年度模型；先用 7 日小算例验证，再运行 365 日。
- 每日储能初始和末端均为 `0.5E`；日期之间无 SOC 连接。
- 每小时保留独立连续变量 `Pch`、`Pdis`；允许同时为正；不得新增充放电状态变量或净储能功率变量。
- 功率平衡中储能贡献写成 `+Pdis-Pch`，但 `Pch` 和 `Pdis` 仍是两个变量。
- 火电第一次求解使用 `0 <= Pf <= Pmax`。
- 第一次结果满足 `Pf < 0.3*Pmax` 的时段在第二次固定为 0；等于阈值时视为开机。
- 第二次开机时段使用原始 `Pmin <= Pf <= Pmax`。
- 最小开停机时间、启动/停机成本当前不实现，必须保留 FUTURE-UC 标记。

### 4.1 正式“日”与 stage_A1 测试夹具的边界（强制）

- 正式物理模型中的“日”只指完整 24 小时；正式 SOC 边界始终为 `SOC(d,s,0)=0.5E(s)`、`SOC(d,s,24)=0.5E(s)`，日期之间无 SOC 连接。
- stage_A1 的第 1 日第 8–10 小时不是物理日，统一称为“3 小时人工闭合测试窗（仅算法测试）”。
- 该测试窗只用于小规模完整 KKT 与递推 Newton 方向的等价性验证。
- 测试窗开始前人为设置 `SOC=0.5E`，不得连接第 7 小时；第 10 小时操作后人为闭合到 `0.5E`。
- 测试窗结果不得解释为正式 24 小时运行结果、经济结果或实际日调度结果。
- 缩短窗口必须显式配置开始小时、结束小时和边界模式；不得从全年数据的 `nHours=24` 隐式推断测试窗末端。

## 5. 数值与算法不变量

- 完整 KKT 审计路线和递推路线必须消费同一个 `linearization` 对象、同一组残差和同一套索引。
- 禁止 `inv`、`pinv`、`lsqminnorm`、显式稠密逆、随机 Hessian、隐藏正则化和自动修改模型。
- 正式递推使用 LDL 和多右端线性求解；不得为每个容量方向重复分解同一块。
- 固定零风光和第二次停机火电必须从活动变量与重合界中删除，并在物理数组中精确恢复为 0。
- 递推方向失败时不得返回完整 KKT 方向冒充递推结果。
- 不得把整日或全年 KKT 转换为 `full`；仅允许对小小时块做诊断性 `full`。

## 6. 阶段门禁

- 只执行 `CURRENT_STAGE.md` 指定阶段。
- 任一阻断性验收失败，状态必须为 `FAIL_RETRYABLE`、`BLOCKED_EXTERNAL` 或 `NEEDS_MODEL_DECISION`，不得进入下一阶段。
- 修复后必须重新执行失败测试和相关回归测试。
- 不得通过放宽阈值、删除测试、修改输入数据或跳过审计获得通过。

## 7. 运行证据

每次运行必须创建唯一 `runs/<run_id>`，记录有效配置、Git 状态、输入哈希、环境、迭代残差、目标组成、步长、索引、块维度、矩阵清单、问题日志、验收结果和中文 Word 报告。历史运行不得覆盖。

大矩阵使用 MAT 或 Matrix Market；CSV 只保存稀疏三元组、清单和小矩阵。所有 CSV 使用稳定列名和至少 17 位有效数字输出浮点数。

## 8. 报告

- 面向用户的正式报告必须是中文 Word 文档，文件名使用中文。
- 报告必须自动引用运行工件，禁止手工编造或复制未验证数值。
- 每份报告必须列出本次实际启用和未启用的目标/约束。
- 每次运行生成摘要；每个阶段生成模型结果报告和问题修复验收报告。

## 9. Git、环境和网络

- 原始 Excel 与受控参考文档只读。
- 每个阶段使用独立分支或工作树；阶段通过后再合并。
- 正式计算阶段默认关闭互联网；确需联网时使用最小域名白名单并记录原因。
- 不执行破坏性 Git 命令，不覆盖用户未提交修改。

## 10. 状态机

允许的终态只有：`PASS`、`FAIL_RETRYABLE`、`BLOCKED_EXTERNAL`、`NEEDS_MODEL_DECISION`。`BLOCKED` 和 `NEEDS_MODEL_DECISION` 都不是成功。
