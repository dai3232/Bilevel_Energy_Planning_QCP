# inputs 目录规则

- `raw` 下文件永久只读，禁止修改、重命名、另存覆盖或用程序写回。
- 每次运行先核对 `数据文件清单与SHA256.csv`。
- Excel 读取必须使用工作表名、区段标签和表头；审计行号不能作为唯一逻辑。
- 基础参数中的“天数=30”不得覆盖输入数据的 365 日。
- 计划曲线必须乘 10000 MW。
- 数据异常必须阻断运行并写入问题日志，不得静默填充、插值或删除。
