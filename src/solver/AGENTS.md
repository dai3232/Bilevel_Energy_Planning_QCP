# src/solver 规则

- 完整 KKT 是审计路线，递推是正式路线；两者消费同一线性化对象。
- 禁止 `inv`、`pinv`、`lsqminnorm`、隐藏正则化、自动对称化和递推失败回退到完整方向。
- 使用稀疏矩阵、LDL 和多右端求解；同一块只分解一次。
- 保留小时链和连续块接口，不得先形成年度稠密 Schur 矩阵。
- 任何奇异、秩亏、NaN/Inf、异常惯性必须记录具体日期、小时、块和索引。
- 正式结果必须逆置换到规范完整方向后再验收。
