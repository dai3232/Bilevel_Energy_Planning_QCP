# src/model 规则

- 只实现 `docs/02_最终模型合同_v1.0.md` 和当前阶段启用矩阵中的内容。
- 变量、等式、不等式顺序必须服从 `docs/07_变量约束索引合同.md`。
- 不创建净储能功率、充放电状态或未批准 0-1 变量。
- 所有残差/Jacobian/Hessian 必须集中在唯一线性化对象中。
- 阶段 A 的 Hessian 必须为精确零；阶段 B/C 的非线性局部 Hessian必须来自真实模型导数。
- 固定零变量必须精确删除并保留恢复映射。
- 模型变更必须同步更新注册表、启用矩阵和测试。
