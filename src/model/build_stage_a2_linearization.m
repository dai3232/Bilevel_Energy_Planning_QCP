function linearization = build_stage_a2_linearization(state,data,index,config)
%BUILD_STAGE_A2_LINEARIZATION A2 wrapper for the shared Stage-A model.
linearization = build_stage_a_linearization(state,data,index,config);
end
