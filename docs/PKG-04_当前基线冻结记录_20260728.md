# PKG-04 当前基线冻结记录（2026-07-28）

## 1. 正式提交基线

- 原项目：`H:\Reproduction\Hourly_Recursive_KKT`
- 原分支：`stage_A4`
- 原 HEAD：`2a70dab184ad38ad4c1b5f5ec50983c357e48397`
- 正式阶段：`stage_A4 / READY`
- 包重构工作树：`H:\Reproduction\Hourly_Recursive_KKT_pkg`
- 包重构分支：`refactor/pkg-interface`

包重构工作树从原 HEAD 创建，没有携带原工作树的未提交修改。

## 2. 原工作树未提交边界

冻结时原工作树存在：

- 6个已跟踪修改文件；
- A4-3F-1 尺度化诊断相关未跟踪 MATLAB/测试文件；
- 人工数据验证原型；
- 历史 runs 和 ZIP；
- 一个来源未归入本阶段的未跟踪 Word 文档。

PKG-0 没有清理、暂存、提交、移动或覆盖这些对象。代码与原型文件的
SHA256 记录在 `PKG-04_当前基线文件SHA256_20260728.csv`。

6个已跟踪修改文件的二进制 diff 文本 SHA256 为：

```text
4be37d4c10eccb541320e96d88313a4551c41712a6539a4199494e91cd2e2a6c
```

计算口径为：读取 `git diff --binary` 的逐行输出，以单个 LF 连接各行，
使用 UTF-8 编码，不在末尾追加换行，再计算 SHA256。

diff 规模为 `6 files changed, 243 insertions(+), 48 deletions(-)`。

## 3. A4-3F-1 数值状态

权威诊断 run：

```text
runs/20260727_121537_stage_A4_3F_1_2a70dab1
```

其终态保持：

- `FAIL_RETRYABLE / FAIL_DIAGNOSTIC`；
- `state_update_count=0`；
- 第21轮冻结点尺度化方向误差约 `9.558e-11`；
- revision 19 方向误差约 `2.006e-10`，超过 `1e-10`；
- 主导失败层为16维 `global_core`；
- 不允许进入 A4-3F-2。

PKG 重构不得改写这一历史结论。

## 4. 数据验证原型

原工作树中已有：

```text
src/data/validation/run_data_import_validation.m
src/data/validation/README_数据导入模块人工检查说明.md
```

原型实际运行结果：

- 正式源数据365日；
- 验证范围第14—20日；
- `7x24=168`小时；
- 风/光数据为 `7x24x5`；
- 所有往返重排最大误差为0；
- 原有数据读取回归 `3/3`；
- Code Analyzer 0项。

该原型留在原工作树。PKG-2 将依据本阶段合同重新引入包接口，并在同一
输入上与原型和正式 `load_project_data` 对照。

## 5. 历史工件

历史 `runs/` 和 ZIP 不进入包重构提交，也不从原工作树移动。PKG-0
只记录它们的存在和权威 run_id，不重新计算历史哈希。

## 6. 基线用途

本记录用于回答：

- 包重构从哪个提交开始；
- 哪些实验代码被有意留在原工作树；
- 新包代码是否意外吸收了未通过的尺度化路线；
- PKG-2 数据接口是否与已运行的数据原型一致；
- 后续移动或清理是否超出授权边界。
