# 阶段A2：1日24小时变维块与固定零：长任务

## 目标

验证第14日24小时风光零可用率删除、自然变维小时块、末端SOC和完整方向等价。

## 数据窗口

第14日1-24时；预期完整KKT维度2749；第19时风3固定零，夜间光伏固定零。

## 前置条件

- `CURRENT_STAGE.md` 指向 `stage_A2` 且状态为 `READY`；对于 stage_E/F 还必须关闭相应决策门禁。
- 上一阶段阻断性验收全部通过。
- 输入哈希、模型合同版本和代码提交已写入 run manifest。

## 本阶段范围

- 固定零精确删除/恢复
- 24小时SOC链
- 最后小时追加末端SOC等式
- 变维块递推

## 明确不在本阶段

- 多日汇总
- 完整内点迭代
- 水量/第二次火电/并行

## 里程碑与执行顺序

1. 扩展A1到24小时
2. 验证活动集和块维度
3. 验证fixed_zero_map
4. 完整KKT与递推比较
5. 保存每小时块诊断

## 必须交付

- A2方向等价证据
- 第14日活动集与块维度CSV
- 中文报告

## 验收

逐项执行 `阶段A2_验收矩阵.csv`。实际值和证据写入运行目录的 `acceptance_results.csv`。任何阻断项失败都不得进入下一阶段。

## 完成条件

- 运行终态为 `PASS`。
- 问题日志中没有未解释的阻断问题。
- 两份中文阶段报告已经从真实工件生成。
- 状态日志记录本阶段最后 run_id、Git commit、测试命令和结论。
- 仅在以上条件满足时，下一阶段可设为 `stage_A3`。
