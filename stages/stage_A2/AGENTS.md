# 阶段A2：1日24小时变维块与固定零 - 阶段规则

- 本目录规则只适用于 `stage_A2`。
- 先读取根 `AGENTS.md`、模型合同、当前阶段长任务和验收矩阵。
- 只实现本阶段范围，不得提前进入 `stage_A3`。
- 所有阻断性验收通过前，不得更新 `CURRENT_STAGE.md`。
- 失败必须按 `docs/10_停止重试阻塞与升级规则.md` 闭环。
- 所有结果写入唯一 `runs/<run_id>`，报告使用中文 Word 和中文文件名。
- 禁止修改原始 Excel、受控参考文档、验收阈值和已冻结模型口径。
