# 阶段F：最小开停机与启动停机成本（延期）：长任务

## 目标

在用户明确后续方案后处理FUTURE-UC-01..05。当前不执行。

## 数据窗口

待定。

## 前置条件

- `CURRENT_STAGE.md` 指向 `stage_F` 且状态为 `READY`；对于 stage_E/F 还必须关闭相应决策门禁。
- 上一阶段阻断性验收全部通过。
- 输入哈希、模型合同版本和代码提交已写入 run manifest。

## 本阶段范围

- 经批准的UC近似或固定掩码修复方案

## 明确不在本阶段

- 当前所有执行任务

## 里程碑与执行顺序

1. 不得开始，直到用户批准并更新模型合同

## 必须交付

- 待定

## 验收

逐项执行 `阶段F_验收矩阵.csv`。实际值和证据写入运行目录的 `acceptance_results.csv`。任何阻断项失败都不得进入下一阶段。

## 完成条件

- 运行终态为 `PASS`。
- 问题日志中没有未解释的阻断问题。
- 两份中文阶段报告已经从真实工件生成。
- 状态日志记录本阶段最后 run_id、Git commit、测试命令和结论。
- 仅在以上条件满足时，下一阶段可设为 `无`。
