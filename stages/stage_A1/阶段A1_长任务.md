# 阶段A1：1日3小时单次Newton方向等价：长任务

## 目标

在第1日8-10时真实数据上建立完整线性化、完整KKT和小时递推，验证一次Newton方向严格等价。

## 数据窗口

第1日，时段8-10；该窗口作为独立测试日，在末时段闭合SOC。预期完整KKT维度471。

## 前置条件

- `CURRENT_STAGE.md` 指向 `stage_A1` 且状态为 `READY`；对于 stage_E/F 还必须关闭相应决策门禁。
- 上一阶段阻断性验收全部通过。
- 输入哈希、模型合同版本和代码提交已写入 run manifest。

## 本阶段范围

- 阶段A仿射模型
- 完整KKT
- 不等式精确消元
- 变维小时块
- LDL多右端
- 全量回代

## 明确不在本阶段

- 完整内点迭代
- 水电日用水
- 第二次火电
- 并行

## 里程碑与执行顺序

1. 实现规范索引
2. 建立唯一linearization
3. 装配完整稀疏KKT
4. 实现小时块D/E/B
5. 实现LDL/Thomas多右端
6. 恢复完整方向
7. 比较方向和代回残差
8. 输出矩阵/索引证据

## 必须交付

- A1方向等价证据
- 完整KKT和小时块清单
- 中文模型与验收报告

## 验收

逐项执行 `阶段A1_验收矩阵.csv`。实际值和证据写入运行目录的 `acceptance_results.csv`。任何阻断项失败都不得进入下一阶段。

## 完成条件

- 运行终态为 `PASS`。
- 问题日志中没有未解释的阻断问题。
- 两份中文阶段报告已经从真实工件生成。
- 状态日志记录本阶段最后 run_id、Git commit、测试命令和结论。
- 仅在以上条件满足时，下一阶段可设为 `stage_A2`。
